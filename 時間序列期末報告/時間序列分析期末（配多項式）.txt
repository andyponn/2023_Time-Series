df<-df_birth$amountt<-1:length(df)df<-as.vector(df)plot(df_birth$amount,type="l")adf.test(df_birth$amount)acf(df, main="ACF ", lag.max = 100)pacf(df, main="PACF ",lag.max = 100)abline(h=0.5, lty=2, col="red")abline(v=20, lty=2, col="green")fit<-lm(df_birth$amount~poly(t,3))summary(fit)res<-fit$residualsplot(df_birth$amount,type="l")lines(predict(fit),col="red")acf(fit$residuals, lag=80)pacf(fit$residuals, lag=80)adf.test(fit$residuals)lmdiffs<-diff(fit$residuals,lag=12)acf(lmdiffs)pacf(lmdiffs)fit4 <- arima(fit$residuals, order=c(2,0,2), seasonal=list(order=c(2,0,1), period=6) )res<-scale(fit4$residuals)Box.test(res, lag=7, type="Ljung-Box", fitdf=7) fit4$aicB_text_p_value = c(0,0)for(hh in 9:20){  B_text_p_value[hh] = Box.test(res, lag=hh, type="Ljung-Box", fitdf=8)$p.value}plot(7:20, B_text_p_value[7:20], type="p",      main="lag3 Ljung-Box statistic",      xlab="lag", ylab="p value", ylim=c(0,1))abline(h=0.05, lty=2, col=4)(1)配lm用法？ 函式、參數？(2)做完季節性差分，一般差分？library(forecast)auto.arima(df_birth$amount,seasonal=TRUE,test="adf",ic="aic")